# Praxis Full Audit — 2026-05-09

Multi-agent review of Rust core, web app, party/WebRTC layer, and mobile/CI/deps. Read-only; no code changed.

Agents: `feature-dev:code-reviewer` × 4, parallel.

---

## Top 10 Priority Fixes

1. **C2** — VP scoring compound bug (`crates/hegemony-core/src/rules/phases.rs:251-252`). Game-breaking from Round 2 onward.
2. **C1** — Convert all `panic!()` in `apply_mutation` to `Result`; add `console_error_panic_hook`.
3. **C5** — Release signing config uses debug keystore (`apps/mobile/android/app/build.gradle:112`).
4. **C6** — Remove committed `debug.keystore`, `.so`, `modules/hegemony/android/build/` from git.
5. **C3 + C4** — Party-mode faction squatting + missing payload size/rate caps.
6. NativeWind PostCSS config (`apps/mobile/postcss.config.js`) — unblocks Phase 5 bundle failure.
7. Move `passBill` rule from TS (`apps/web/lib/store/index.ts:300-309`) into Rust.
8. Rename `app/error.tsx` → `app/global-error.tsx`; surface WASM bootstrap failures.
9. CI: add mobile typecheck + `cargo check --target aarch64-linux-android` + WASM rebuild.
10. Next.js 14.2.15 → 15.x (CVE exposure on SSR / Server Actions).

---

## CRITICAL

| # | File:Line | Problem |
|---|---|---|
| C1 | `crates/hegemony-core/src/mutations.rs` (10+ sites: 186-190, 213-214, 244-246, 258-260, 364-365, 378-380, 395-397, 456-458, 471-472, 59, 583-584) | `panic!()` arms inside `apply_mutation` for invalid `ClassId` combos. WASM panic = `RuntimeError: unreachable`, no recovery, no JS-visible error. Stale snapshot in `localStorage` panics undo. |
| C2 | `crates/hegemony-core/src/rules/phases.rs:251-252` | `apply_scoring_phase` overwrites `vp` with `vp_for(...).total` which already contains prior `vp`. VP compounds nonsense from Round 2. Game-breaking. |
| C3 | `apps/web/lib/store/index.ts:457-466` | Faction squatting. No uniqueness check on `select_faction`; last writer wins, stolen seat → mutations honored. |
| C4 | `packages/party/src/room-host.ts:62-65` | No payload size or rate cap on inbound peer messages. One peer can flood/oversize → freeze host tab → kill session. |
| C5 | `apps/mobile/android/app/build.gradle:112` | `release` buildType signs with `signingConfigs.debug`. Any release APK/AAB rejected on Play Store. |
| C6 | `apps/mobile/android/app/debug.keystore` + `apps/mobile/modules/hegemony/android/build/` + `jniLibs/*.so` | Binaries committed. Stale `.so` shadows fresh Rust build silently → ABI mismatch crash. No `.gitignore` in module dir. |

---

## HIGH

### Rust core / WASM
- `wasm.rs:14-18` — `console_error_panic_hook` deliberately omitted. Every panic invisible in DevTools.
- `mutations.rs:524-561` — `ApplyEndRound` trusts JS payload blindly. UI can grant Working free money without deducting employers. Compute server-side.
- `jni_exports.rs:13-18` — `.expect()` on null `JString` and OOM in `rust_to_jstring` aborts JVM, no Java exception.
- `crates/hegemony-core/src/rules/taxes.rs:169-175` — `count_middle_workers_in_other_companies` is a TODO heuristic that over-counts Middle income tax.

### Web app
- `lib/store/index.ts:300-309` — `passBill` hard-codes `+3 VP` rule in TS. CLAUDE.md forbids. Move to Rust.
- `lib/store/index.ts:74-96` — `classPathToMutation` mirrors Rust domain knowledge silently; will drift.
- `app/error.tsx` — Wrong filename. Next.js needs `app/global-error.tsx` with own `<html><body>` to catch root-layout errors.
- `app/_components/WasmBootstrap.tsx:8` — WASM init failure swallowed via `.catch(console.error)`. Network/CORS failure → silent panel cascade.
- `lib/store/index.ts:131` — `as GameState` cast with no shape validation. Bad WASM return = silently corrupt state.
- `app/play/import/page.tsx:17` + `SavedGamesList.tsx:36` — `btoa(unescape(encodeURIComponent(...)))` and `decodeURIComponent(escape(atob(...)))`. Deprecated, breaks on emoji + non-ASCII.
- `components/shared/Modal.tsx:21-56` — `onClose` in effect deps, identity changes every parent render → effect re-runs.
- `lib/store/index.ts:685` — Selector reads module-level `hostInstance`/`peerInstance` outside Zustand reactive scope.
- `app/sw.ts:19` — Confirm `*.wasm` is content-hashed; otherwise stale WASM served indefinitely after deploy.

### Party / WebRTC
- `lib/store/index.ts:136-137,483` — Full `GameState` broadcast to every peer. Future hidden cards/secret votes leak. Need `stateForPeer(state, faction)` projection.
- `packages/party/src/room-code.ts:13-15` — Modulo bias: `buf[i] % 31` skews first 8 chars. Use rejection sampling.
- `packages/party/src/room-peer.ts:81-88` — Peer trusts any incoming message including `host_leaving`; no host-ID verification.
- `lib/store/index.ts:467-478` — `mutation_request` cast `as Mutation` reaches WASM with no `type` whitelist. Unknown variants → panic.

### Deps / CI
- `apps/web/package.json:21` — `next: 14.2.15`. Two majors behind. CVEs against Next 14 SSR/Server Actions in 2024-25. Upgrade to 15.x.
- web `zustand@^4.5.5` vs mobile `^5.0.3` — different majors, breaking store API.
- `.github/workflows/ci.yml` — No mobile job. No `cargo check --target aarch64-linux-android`. JNI surface never compiled in CI.
- `apps/mobile/metro.config.js:22` + missing `apps/mobile/postcss.config.js` — Root cause of Phase 5 nativewind bundle failure. NativeWind 4 needs PostCSS pipeline.
- `apps/mobile/modules/hegemony` not in `pnpm-workspace.yaml`. Resolved only via Metro `watchFolders`. Frozen-lockfile CI won't symlink.
- `packages/hegemony-wasm/*.{wasm,js}` committed binaries; CI doesn't rebuild → silent staleness.

---

## MEDIUM

### Rust
- `mutations.rs:19-22` + `starting_state.rs:5-18` — `now_ms()` returns `0` on WASM. All history timestamps identical. Use `js_sys::Date::now()`.
- `packages/hegemony-wasm/hegemony_core.d.ts` — All signatures typed `any → any`. Zero TS type safety on `GameState`/`Mutation` boundary.
- Duplicated `now_ms()` across `mutations.rs` and `starting_state.rs`.
- `Cargo.toml:7` — `cdylib` always built even on `cargo test`.

### Web
- `EndRoundWizard.tsx:20-22` — `wasm()` inside `useMemo` can throw before init resolves on slow connection.
- `ClassPanelShell.tsx:63` — `compute_vp_wasm` unmemoized, fires every render × 4 panels. Party-mode = main-thread block.
- `sentry.client.config.ts:12` — `enabled: NODE_ENV === "production"` disables Sentry on Vercel previews.
- `lib/store/index.ts:325-333` — TS-side bounds clamping duplicates Rust. Negative input silently → 0.
- `app/manifest.ts:22-26` — `apple-icon` declared `purpose: maskable` without safe-zone padding audit.
- `components/shared/HideCurtain.tsx:17` — Blur curtain not a security boundary; document.
- `app/play/[gameId]/page.tsx:47-51` — `useEffect` deps include `state`; over-subscribes on every counter tap.

### Party
- `app/play/lobby/page.tsx:46-49` — `joinUrl` puts code in `?code=` query string → leaks via Referer + browser history. Use `#code=`.
- `room-host.ts:49-61` — No `MAX_PEERS` cap. Unlimited connections → memory exhaustion.
- `lib/store/index.ts:457-463` — `hello`/`select_faction` accepted post-game-start. Peer can change faction mid-game.
- `app/party-test/page.tsx:1` — Lab/debug page publicly routable in production.

### Mobile / Deps / CI / Docs
- `eas.json` cache uses `../../crates/hegemony-core/target` — relative traversal above project root, unreliable.
- `apps/mobile/scripts/build-rust-android.sh` — no `cargo-ndk` install guard.
- `apps/mobile/android/app/src/main/AndroidManifest.xml:5-6` — Declares `READ/WRITE_EXTERNAL_STORAGE`. Not needed; Android 13+ rejects.
- mobile TypeScript `~5.3.3` vs web `^5.5.3` — align.
- `ci.yml` Rust job runs no Android-target check; tests host config only.
- `ci.yml` no `node-version` lock via `.nvmrc`/`engines`.
- `apps/web/.gitignore` only `.vercel`. Add `.env*` belt-and-suspenders.
- `modules/hegemony/android/build/` logs leak absolute home dir paths in manifest merger output.
- `README.md` — describes npm + flat repo; reality is pnpm monorepo + Rust + WASM + Expo. Fully stale.
- `CLAUDE.md` — "Step 2: UI redesign" stale; web Phase 6+9 done, mobile paused.

---

## LOW

- `cards/data.rs:110-120` — Five `P_FOOD_*` `#[allow(dead_code)]` placeholder statics.
- `Cargo.toml:25` — `proptest = "1"` no minor floor.
- `proptest_tests.rs:131-163` — Only exercises `AdjustMoney { Working }`. Won't catch C1 panics.
- `rules/mod.rs:8-13` — Blanket `pub use *` flattens namespace.
- `OnboardingModal.tsx:55-63` — `role="tablist"` on non-interactive `<span>` step indicator.
- `SavedGamesList.tsx:55` — Redundant `i` flag on regex with `a-z` range.
- `lib/types/game.ts` — `VpBreakdown` duplicated across `ClassPanelShell` and `ScoringPanel`.
- store `apply()` silently drops peer mutations for unowned classes; no UI feedback.
- `eslint@^8.57.0` + `eslint-config-next@14.2.15` on EOL ESLint v8.
- `ErrorBoundary.tsx:25` — Sentry `extra` could capture state. Add `beforeSend` strip hook.
- `AGENTS.md` only Vercel boilerplate, no Praxis context.

---

## Party-Mode Threat Model Snapshot

- ✅ Rogue peer **cannot** directly hit WASM with another faction's mutation (host validates owner).
- ❌ Rogue peer **can** squat unclaimed/claimed faction in lobby (C3).
- ❌ Rogue peer **can** flood host (C4).
- ❌ Rogue peer **can** read all other players' secrets once any private-info field is added (full state broadcast).
- ⚠️ Brute-force room-code online ≈ infeasible (31^6 ≈ 887M codes, slow PeerJS handshake), but alphabet biased.

---

## Dep CVE Scan — 2026-05-09

### `pnpm audit --prod`
**27 vulnerabilities — 1 critical, 15 high, 9 moderate, 2 low.** Almost entirely `next` 14.2.15 + transitive `node-tar`, `xmldom`, `postcss`.

**CRITICAL**
- `next` `>=14.0.0 <14.2.25` — Authorization Bypass in Middleware (GHSA-f82v-jwr5-mffw). Direct hit on `apps/web`; if any auth/route gating is added later this is exploitable today.

**HIGH (selected — all gated by Next 14.2.15 or transitive deps)**
- `next` ≥3 DoS via Server Components (GHSA series, all patched ≥14.2.34)
- `next` HTTP request deserialization DoS
- `node-tar` × 6 — arbitrary file overwrite, hardlink/symlink path traversal, drive-relative path traversal, race condition (transitive via expo build tools)
- `xmldom` × 5 — XML injection via CDATA, uncontrolled recursion, node injection (transitive via expo / Android tooling)

**MODERATE**
- `next` content injection in next/image optimizer (<14.2.31)
- `next` self-hosted DoS via Image Optimizer remotePatterns (<15.5.10)
- `next` HTTP request smuggling in rewrites (<15.5.13)
- `next` unbounded next/image disk cache (<15.5.14)
- `postcss` <8.5.10 XSS via unescaped `</style>` (transitive: web + mobile expo)

**LOW**
- `next` info exposure in dev server origin (<14.2.30)
- `next` race condition cache poisoning (<14.2.24)

**Fix:** Upgrade `apps/web` to Next.js 15.5.14+ (covers all 13 `next` advisories above). Most `node-tar` / `xmldom` items resolve via `expo` upgrade in `apps/mobile`. `postcss` resolves via deduped overrides or upstream upgrades.

### `cargo audit`
**0 vulnerabilities** across 107 Rust crates.

Note: `Cargo.lock` was **not committed** before this scan — generated fresh. Recommended action: commit `crates/hegemony-core/Cargo.lock` for reproducible builds and reproducible future audits.

---

## Skipped / Recommended Next

- License / compliance review
- Perf profiling
- Accessibility deep-dive (axe sweep already in repo)
- Re-run audits after Next.js 15 upgrade to confirm clean tree
